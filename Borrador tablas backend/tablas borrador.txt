SHOW DATABASES;
CREATE DATABASE TIENDADECAFE;
DROP DATABASE TIENDADECAFE;
CREATE DATABASE TIENDADECAFE; 
USE TIENDADECAFE;


DROP TABLE IF EXISTS USUARIOS;


CREATE TABLE USUARIOS (
    ID INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
    NOMBRE VARCHAR(100) NOT NULL,
    USUARIO VARCHAR(100) NOT NULL,
    EMAIL VARCHAR(100) NOT null,
    PAIS VARCHAR(100) not null,
    PASSWORD VARCHAR (255) not NULL
);


INSERT INTO USUARIOS (NOMBRE, USUARIO, EMAIL,PAIS,PASSWORD) VALUES ('MARÍA VICTORIA GÓMEZ', 'VICKY88', 'mariavictoriagomez1988@gmail.com', 'Argentina','lalelilolu');



DROP TABLE IF EXISTS PRODUCTOS;

CREATE TABLE PRODUCTOS (
    ID INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
    NOMBRE VARCHAR(100) NOT NULL,
    DESCRIPCION VARCHAR(150) NOT NULL,
    PRECIO DOUBLE not NULL,
    STOCK DOUBLE
);

INSERT INTO PRODUCTOS (NOMBRE, DESCRIPCION, PRECIO, STOCK) VALUES ('CAFE', 'CAFE DE BRASIL MOLIDO', 25, 30);

create table SERVICIOS (
ID INT auto_increment primary key not null,
NOMBRE VARCHAR(100) not null,
DESCRIPCION VARCHAR (150),
PRECIO DOUBLE not null
);

insert into SERVICIOS (NOMBRE, DESCRIPCION, PRECIO) values ('CAFETERÍA MOVIL', 'Celebrá momentos únicos con nuestra impecable oferta de café',100000)

CREATE TABLE PEDIDOS (
    ID INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
    ID_USUARIO INT NOT NULL,
    ID_PRODUCTO INT NOT NULL,
    CANTIDAD INT NOT NULL,
    FECHA_PEDIDO DATE NOT NULL,
    FOREIGN KEY (ID_USUARIO) REFERENCES USUARIOS(ID),
    FOREIGN KEY (ID_PRODUCTO) REFERENCES PRODUCTOS(ID)
);

insert into PEDIDOS (ID_USUARIO, ID_PRODUCTO,CANTIDAD,FECHA_PEDIDO) values (1,1,5,'2024-09-25')